export interface IUser {
  name: {
    firstName: string;
    lastName: string;
  };
  userName: string;
  phoneNumber: string;
  email: string;
  gender: string;
  password: string;
  address: string;
}
